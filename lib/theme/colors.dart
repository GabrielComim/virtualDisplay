import 'package:flutter/material.dart';

// Note: No código de cor o FF inicial é relacionado a opacidade da cor.
class AppColors {
  static const primary = Color(0xFF010426);
  static const secondary = Color(0xFF00D4FF);
  static const tertiary = Color(0x550A0F4D);
  static const background = Color(0xFF050816);
  static const surface = Color(0xFF10152B);
  static const error = Color(0xFFE53935);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B8D4);
  static const buttons = Color(0xFF6A8DB5);
  static const outline = Color(0xFF1F2747);
  static const outlineVariant = Color(0xAAFFFFFF);
  // Para usar: Precisa chamar AppColors.icons
  static const icons = Color(0xFF03355D);
  static const cardConnectionBackground = Color(0x4DAFB0C3);  // Opacidade em 30%
}